/***
    AUTHOR:    shabylkhan
    CREATED:   05.11.2022 15:47:32

***/

#include<bits/stdc++.h>

using namespace std;

void solve() {
	int n;
	cin >> n;
	while (n--) {
		string tp;
		int x, y;
		cin >> tp >> x >> y;
		if (tp == "add") {
			s[x].push
		}
	}
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int tt = 1;
	while (tt--) {
		solve();
	}

	return 0;
} 