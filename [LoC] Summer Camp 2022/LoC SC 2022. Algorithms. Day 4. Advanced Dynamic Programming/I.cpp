/***
    AUTHOR:    shabylkhan
    CREATED:   21.07.2022 18:27:46

***/

#include<bits/stdc++.h>

using namespace std;

void solve() {
	int n;
	cin >> n;
	for (int i = 0; i < n; i++) {
		 cin >> edge[i].first.first >> edge[i].first.second >> edge[i].second;

	}
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int tt = 1;
	while (tt--) {
		solve();
	}

	return 0;
} 